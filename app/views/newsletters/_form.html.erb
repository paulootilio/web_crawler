<%= form_with(model: newsletter, local: true) do |form| %>
  <% if newsletter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newsletter.errors.count, "error") %> prohibited this newsletter from being saved:</h2>

      <ul>
      <% newsletter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fonte %>
    <%= form.text_field :fonte %>
  </div>

  <div class="field">
    <%= form.label :url_noticia %>
    <%= form.text_field :url_noticia %>
  </div>

  <div class="field">
    <%= form.label :titulo_noticia %>
    <%= form.text_field :titulo_noticia %>
  </div>

  <div class="field">
    <%= form.label :data_publicacao %>
    <%= form.datetime_select :data_publicacao %>
  </div>

  <div class="field">
    <%= form.label :data_coleta %>
    <%= form.datetime_select :data_coleta %>
  </div>

  <div class="field">
    <%= form.label :corpo_noticia %>
    <%= form.text_field :corpo_noticia %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
